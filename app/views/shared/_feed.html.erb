<% if @feed_items.any? %>
  <ol class="microposts">
    <% # Here Rails knows to use microposts/_micropost partial. Its because
       # @feed_items are of class Micropost. 
    %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
